# === Базовые библиотеки ===
PyPDF2==3.0.1             # Извлечение текста из PDF
pdfplumber==0.11.0        # Альтернатива PyPDF2 (если нужно более точное извлечение)
python-dotenv==1.0.1      # Загрузка переменных окружения из .env
tqdm==4.66.5              # Прогресс-бары при обработке файлов
pandas==2.2.3             # Для работы с таблицами и метаданными чанков
numpy==1.26.4             # Для векторных операций и индексации
faiss-cpu==1.10.0          # FAISS — для векторного поиска (можно заменить на faiss-gpu)
transformers==4.53.0      # Для LLM и моделей эмбеддингов
torch==2.4.0              # PyTorch — фреймворк для моделей
accelerate==0.34.2        # Оптимизация вычислений (если будет использоваться GPU)

# === Веб / интерфейс (на будущее) ===
fastapi==0.115.0          # Для API-интерфейса чат-бота
uvicorn==0.30.6           # ASGI-сервер для FastAPI
