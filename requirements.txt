# === Базовые библиотеки ===
PyPDF2==3.0.1             # Извлечение текста из PDF
pdfplumber==0.11.0        # Альтернатива PyPDF2 для более точного извлечения
python-dotenv==1.0.1      # Загрузка переменных окружения из .env
tqdm==4.66.5              # Прогресс-бары при обработке файлов
pandas==2.2.3             # Работа с таблицами, CSV, JSON
numpy==1.26.4             # Векторные операции и индексация
faiss-cpu==1.10.0         # FAISS — векторный поиск (для CPU)
sentence-transformers==3.1.1  # Для эмбеддингов текстов
scikit-learn==1.5.2       # Для TF-IDF, cosine_similarity и классических методов

# === NLP / LLM ===
transformers==4.53.0      # HuggingFace Transformers (LLM, токенизация)
torch==2.4.0              # PyTorch backend
accelerate==0.34.2        # Оптимизация вычислений (GPU / CPU)
pymorphy2==0.9.1          # Лемматизация русского языка
razdel==0.5.0             # Быстрая токенизация русского текста (при необходимости)
nltk==3.9.1               # Разбиение текста на предложения / токены
symspellpy==6.7.7         # Быстрая орфокоррекция (опционально)
# jamspell==0.0.12         # Альтернатива для проверки орфографии (если нужен бинарный пакет)

# === Веб / интерфейс (опционально) ===
fastapi==0.115.0          # Для API-интерфейса RAG или чат-бота
uvicorn==0.30.6           # ASGI-сервер для FastAPI

# === Вспомогательные пакеты ===
loguru==0.7.2             # Красивое логирование
rich==13.9.3              # Цветной вывод и красивые прогресс-бары
